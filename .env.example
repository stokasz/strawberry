# Production config lives in config/*.env
# Created by: pnpm strawberry onboard

# See:
#   config/strawberry.env.example
#   config/host.env.example
#   config/telegram.env.example
#   config/agent.env.example

# Unit tests may still use process env overrides directly.
# The production stack always uses Apple Container isolation.
